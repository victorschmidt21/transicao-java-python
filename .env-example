# ==============================
# DJANGO CONFIG
# ==============================
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=troque_essa_chave
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Nome do projeto (pode ser usado em configurações personalizadas)
PROJECT_NAME=meu_projeto

# ==============================
# DATABASE - LOCAL (SQLite)
# ==============================
# Usado em desenvolvimento
SQLITE_NAME=db.sqlite3

# ==============================
# DATABASE - PRODUÇÃO (PostgreSQL)
# ==============================
# Quando for para produção, troque o ENGINE e as variáveis correspondentes
# ==============================
# EMAIL (Opcional)
# ==============================
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.exemplo.com
EMAIL_PORT=587
EMAIL_HOST_USER=usuario@exemplo.com
EMAIL_HOST_PASSWORD=senha_do_email
EMAIL_USE_TLS=True